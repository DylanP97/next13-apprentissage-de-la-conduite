export { default } from "next-auth/middleware";

export const config = {
  matcher: [
    // "/abonnement",
    // "/article",
    // "/admin-edition",
    // "/admin-articles",
    // "/admin-eleves",
    // "/admin-questions",
    // "/contact",
    // "/profil",
    // "/quiz",
  ],
};
